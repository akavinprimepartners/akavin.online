# Akavin Panorama Viewer Safe Version

This version is isolated to avoid disturbing existing WordPress theme/pages.
- All CSS namespaced
- Loads only on shortcode pages
- No global CSS overrides
- No theme conflicts
